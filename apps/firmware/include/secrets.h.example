#pragma once

// Copy to secrets.h (gitignored) and fill in your values.

#define BACKEND_HOST "192.168.1.202"
#define BACKEND_PORT 8000

// ESP32-C3 is 2.4 GHz only — not *_5G
#define WIFI_STA_SSID "YourHomeWiFi-2.4G"
#define WIFI_STA_PASSWORD "your-wifi-password"

// Leave empty — firmware calls POST /v1/auth/device-login when STA is up
#define GATEWAY_JWT_TOKEN ""
